# Deployer for the vault. Needs >= 33 STT: 32 sits in the vault as the
# reactivity sybil gate (never consumed), the rest pays deploy + handler gas.
PRIVATE_KEY=0x
SHANNON_RPC=https://api.infra.testnet.somnia.network/
