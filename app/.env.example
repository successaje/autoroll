# Set this to a deployed AutoRollVault and the app drives it from the user's
# wallet. Leave it unset and the app drives the off-chain roller instead, which
# is how the demo runs before the vault is deployed.
VITE_VAULT_ADDRESS=

# Override only when pointing at a local fork (e.g. anvil --fork-url shannon).
VITE_RPC_URL=
